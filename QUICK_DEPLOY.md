# 🚀 БЫСТРЫЙ ДЕПЛОЙ ЗА 5 МИНУТ

## Вариант 1: Render.com (САМЫЙ ПРОСТОЙ) ⭐

### Шаг 1: Загрузите код на GitHub (2 минуты)
```bash
# Если еще не сделали:
git init
git add .
git commit -m "Ready for deployment"
git branch -M main

# Создайте репозиторий на github.com и выполните:
git remote add origin https://github.com/ВАШ_USERNAME/kindergarten.git
git push -u origin main
```

### Шаг 2: Деплой на Render (3 минуты)

1. **Зайдите на https://render.com** и войдите через GitHub

2. **Создайте PostgreSQL базу данных:**
   - Нажмите "New +" → "PostgreSQL"
   - Name: `kindergarten-db`
   - Plan: **Free**
   - Нажмите "Create Database"
   - **Скопируйте** Internal Database URL (будет что-то вроде: `postgresql://user:pass@host:5432/dbname`)

3. **Создайте Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий `kindergarten`
   - Нажмите "Connect"

4. **Настройки сервиса:**
   ```
   Name: kindergarten-app
   Environment: Java
   Region: Singapore (или ближайший к вам)
   Branch: main
   Root Directory: (оставьте пустым)
   Build Command: mvn clean package -DskipTests
   Start Command: java -jar target/*.jar
   Instance Type: Free
   ```

5. **Добавьте переменные окружения:**
   Нажмите "Advanced" → "Add Environment Variable" и добавьте:
   
   ```
   DATABASE_URL = (вставьте Internal Database URL из шага 2)
   DATABASE_USERNAME = (из URL, обычно после postgresql://)
   DATABASE_PASSWORD = (из URL)
   SPRING_MAIL_HOST = smtp.gmail.com
   SPRING_MAIL_PORT = 587
   SPRING_MAIL_USERNAME = ваш_email@gmail.com
   SPRING_MAIL_PASSWORD = ваш_пароль_приложения_gmail
   JWT_SECRET = 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
   PORT = 8080
   ```

   **Как получить пароль приложения Gmail:**
   - Зайдите на https://myaccount.google.com/security
   - Включите "Двухэтапную аутентификацию" (если еще не включена)
   - Перейдите в "Пароли приложений"
   - Создайте новый пароль для "Почта"
   - Используйте этот пароль в `SPRING_MAIL_PASSWORD`

6. **Нажмите "Create Web Service"**

7. **Ждите 5-10 минут** пока Render соберет и задеплоит приложение

8. **Готово!** Ваше приложение будет доступно по адресу:
   `https://kindergarten-app.onrender.com` (или другой URL, который покажет Render)

---

## Вариант 2: Railway.app (ЕЩЕ ПРОЩЕ) ⚡

1. Зайдите на https://railway.app
2. Войдите через GitHub
3. Нажмите "New Project" → "Deploy from GitHub repo"
4. Выберите ваш репозиторий
5. Railway автоматически определит Java проект
6. Нажмите "Add Service" → "Database" → "PostgreSQL"
7. Railway автоматически добавит переменные окружения
8. Добавьте только:
   - `SPRING_MAIL_USERNAME`
   - `SPRING_MAIL_PASSWORD`
   - `JWT_SECRET`
9. Готово! Railway автоматически задеплоит

---

## Вариант 3: Heroku (Классический)

```bash
# Установите Heroku CLI: https://devcenter.heroku.com/articles/heroku-cli

heroku login
heroku create kindergarten-app
heroku addons:create heroku-postgresql:hobby-dev
heroku config:set SPRING_MAIL_USERNAME=ваш_email@gmail.com
heroku config:set SPRING_MAIL_PASSWORD=ваш_пароль
heroku config:set JWT_SECRET=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
git push heroku main
```

---

## ✅ Проверка работы

После деплоя откройте в браузере:
- Главная: `https://ваш-домен.onrender.com`
- Вход: `https://ваш-домен.onrender.com/login.html`
- Регистрация: `https://ваш-домен.onrender.com/register.html`

## 🆘 Проблемы?

**Приложение не запускается:**
- Проверьте логи в Render Dashboard → Logs
- Убедитесь, что все переменные окружения установлены
- Проверьте, что база данных создана

**Email не работает:**
- Убедитесь, что используете "Пароль приложения" Gmail, а не обычный пароль
- Проверьте, что двухфакторная аутентификация включена

**База данных не подключается:**
- Используйте Internal Database URL (не External)
- Убедитесь, что база данных в том же регионе

---

**🎉 После деплоя ваше приложение будет доступно из любого места в мире 24/7!**

